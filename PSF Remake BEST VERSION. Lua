-- PSF Remake 3.0
-- Ultimate Script Hub with Premium Design

local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")

local player = Players.LocalPlayer
local playerGui = player:WaitForChild("PlayerGui")

-- Очистка предыдущего GUI
if playerGui:FindFirstChild("PSFRemake3") then
    playerGui.PSFRemake3:Destroy()
end

-- Создание основного GUI
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "PSFRemake3"
ScreenGui.ResetOnSpawn = false
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

-- Адаптивный контейнер
local MainContainer = Instance.new("Frame")
MainContainer.Name = "MainContainer"
MainContainer.Size = UDim2.new(0.8, 0, 0.7, 0)
MainContainer.Position = UDim2.new(0.1, 0, 0.15, 0)
MainContainer.AnchorPoint = Vector2.new(0, 0)
MainContainer.BackgroundColor3 = Color3.fromRGB(15, 15, 20)
MainContainer.BorderSizePixel = 0

if UserInputService.TouchEnabled then
    MainContainer.Size = UDim2.new(0.95, 0, 0.9, 0)
    MainContainer.Position = UDim2.new(0.025, 0, 0.05, 0)
end

local ContainerCorner = Instance.new("UICorner")
ContainerCorner.CornerRadius = UDim.new(0, 12)
ContainerCorner.Parent = MainContainer

local ContainerShadow = Instance.new("ImageLabel")
ContainerShadow.Name = "ContainerShadow"
ContainerShadow.Size = UDim2.new(1, 20, 1, 20)
ContainerShadow.Position = UDim2.new(0, -10, 0, -10)
ContainerShadow.BackgroundTransparency = 1
ContainerShadow.Image = "rbxassetid://5554236805"
ContainerShadow.ImageColor3 = Color3.fromRGB(0, 0, 0)
ContainerShadow.ImageTransparency = 0.8
ContainerShadow.ScaleType = Enum.ScaleType.Slice
ContainerShadow.SliceCenter = Rect.new(23, 23, 277, 277)

-- Хедер для перемещения
local Header = Instance.new("Frame")
Header.Name = "Header"
Header.Size = UDim2.new(1, 0, 0, 50)
Header.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
Header.BorderSizePixel = 0
Header.Active = true

local HeaderGradient = Instance.new("UIGradient")
HeaderGradient.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0, Color3.fromRGB(45, 45, 55)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(25, 25, 35))
})
HeaderGradient.Rotation = -15
HeaderGradient.Parent = Header

local HeaderCorner = Instance.new("UICorner")
HeaderCorner.CornerRadius = UDim.new(0, 12)
HeaderCorner.Parent = Header

local Title = Instance.new("TextLabel")
Title.Name = "Title"
Title.Size = UDim2.new(0, 300, 1, 0)
Title.Position = UDim2.new(0, 20, 0, 0)
Title.BackgroundTransparency = 1
Title.Text = "🚀 PSF REMAKE 3.0"
Title.TextColor3 = Color3.fromRGB(220, 220, 255)
Title.TextSize = 18
Title.Font = Enum.Font.GothamBold
Title.TextXAlignment = Enum.TextXAlignment.Left

local SubTitle = Instance.new("TextLabel")
SubTitle.Name = "SubTitle"
SubTitle.Size = UDim2.new(0, 300, 0, 20)
SubTitle.Position = UDim2.new(0, 20, 0, 25)
SubTitle.BackgroundTransparency = 1
SubTitle.Text = "Ultimate Script Hub • Drag to move"
SubTitle.TextColor3 = Color3.fromRGB(150, 150, 180)
SubTitle.TextSize = 11
SubTitle.Font = Enum.Font.Gotham
SubTitle.TextXAlignment = Enum.TextXAlignment.Left

-- Основной контент
local Content = Instance.new("Frame")
Content.Name = "Content"
Content.Size = UDim2.new(1, 0, 1, -50)
Content.Position = UDim2.new(0, 0, 0, 50)
Content.BackgroundTransparency = 1

-- Левая панель - редактор
local EditorPanel = Instance.new("Frame")
EditorPanel.Name = "EditorPanel"
EditorPanel.Size = UDim2.new(0.65, -10, 1, -10)
EditorPanel.Position = UDim2.new(0, 10, 0, 10)
EditorPanel.BackgroundColor3 = Color3.fromRGB(25, 25, 30)
EditorPanel.BorderSizePixel = 0

local EditorCorner = Instance.new("UICorner")
EditorCorner.CornerRadius = UDim.new(0, 8)
EditorCorner.Parent = EditorPanel

local EditorHeader = Instance.new("Frame")
EditorHeader.Name = "EditorHeader"
EditorHeader.Size = UDim2.new(1, 0, 0, 40)
EditorHeader.BackgroundColor3 = Color3.fromRGB(35, 35, 40)
EditorHeader.BorderSizePixel = 0

local EditorTitle = Instance.new("TextLabel")
EditorTitle.Name = "EditorTitle"
EditorTitle.Size = UDim2.new(0, 200, 1, 0)
EditorTitle.Position = UDim2.new(0, 15, 0, 0)
EditorTitle.BackgroundTransparency = 1
EditorTitle.Text = "📝 PSF EDITOR"
EditorTitle.TextColor3 = Color3.fromRGB(220, 220, 255)
EditorTitle.TextSize = 14
EditorTitle.Font = Enum.Font.GothamBold
EditorTitle.TextXAlignment = Enum.TextXAlignment.Left

-- Элементы редактора
local EditorContainer = Instance.new("Frame")
EditorContainer.Name = "EditorContainer"
EditorContainer.Size = UDim2.new(1, -20, 1, -100)
EditorContainer.Position = UDim2.new(0, 10, 0, 50)
EditorContainer.BackgroundColor3 = Color3.fromRGB(10, 10, 15)
EditorContainer.BorderSizePixel = 0

local EditorContainerCorner = Instance.new("UICorner")
EditorContainerCorner.CornerRadius = UDim.new(0, 6)
EditorContainerCorner.Parent = EditorContainer

local EditorScroll = Instance.new("ScrollingFrame")
EditorScroll.Name = "EditorScroll"
EditorScroll.Size = UDim2.new(1, 0, 1, 0)
EditorScroll.BackgroundTransparency = 1
EditorScroll.ScrollBarThickness = 8
EditorScroll.CanvasSize = UDim2.new(0, 0, 10, 0)
EditorScroll.VerticalScrollBarInset = Enum.ScrollBarInset.Always

local EditorBox = Instance.new("TextBox")
EditorBox.Name = "EditorBox"
EditorBox.Size = UDim2.new(1, -10, 1, -10)
EditorBox.Position = UDim2.new(0, 5, 0, 5)
EditorBox.BackgroundTransparency = 1
EditorBox.Text = [[-- PSF Remake 3.0
-- Ultimate Script Hub

print("🚀 PSF Remake 3.0 Activated!")
print("🎯 Premium Script Framework")]]
EditorBox.TextColor3 = Color3.fromRGB(220, 220, 220)
EditorBox.TextSize = 13
EditorBox.Font = Enum.Font.Code
EditorBox.TextXAlignment = Enum.TextXAlignment.Left
EditorBox.TextYAlignment = Enum.TextYAlignment.Top
EditorBox.TextWrapped = true
EditorBox.MultiLine = true
EditorBox.ClearTextOnFocus = false

-- Кнопки редактора
local EditorTools = Instance.new("Frame")
EditorTools.Name = "EditorTools"
EditorTools.Size = UDim2.new(1, -20, 0, 40)
EditorTools.Position = UDim2.new(0, 10, 1, -50)
EditorTools.BackgroundTransparency = 1

local function CreateToolButton(name, text, position, color, icon)
    local button = Instance.new("TextButton")
    button.Name = name
    button.Size = UDim2.new(0, 100, 0, 32)
    button.Position = position
    button.BackgroundColor3 = color
    button.Text = " " .. text
    button.TextColor3 = Color3.fromRGB(255, 255, 255)
    button.TextSize = 12
    button.Font = Enum.Font.GothamBold
    button.TextXAlignment = Enum.TextXAlignment.Left
    
    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, 6)
    corner.Parent = button
    
    local iconLabel = Instance.new("TextLabel")
    iconLabel.Name = "Icon"
    iconLabel.Size = UDim2.new(0, 20, 0, 20)
    iconLabel.Position = UDim2.new(0, 8, 0.5, -10)
    iconLabel.BackgroundTransparency = 1
    iconLabel.Text = icon
    iconLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
    iconLabel.TextSize = 14
    iconLabel.Font = Enum.Font.GothamBold
    iconLabel.Parent = button
    
    return button
end

local ExecuteBtn = CreateToolButton("ExecuteBtn", "EXECUTE", UDim2.new(0, 0, 0, 0), Color3.fromRGB(0, 150, 100), "▶")
local ClearBtn = CreateToolButton("ClearBtn", "CLEAR", UDim2.new(0, 110, 0, 0), Color3.fromRGB(150, 100, 0), "🗑️")

-- Правая панель - скрипты
local ScriptsPanel = Instance.new("Frame")
ScriptsPanel.Name = "ScriptsPanel"
ScriptsPanel.Size = UDim2.new(0.35, -10, 1, -10)
ScriptsPanel.Position = UDim2.new(0.65, 10, 0, 10)
ScriptsPanel.BackgroundColor3 = Color3.fromRGB(25, 25, 30)
ScriptsPanel.BorderSizePixel = 0

local ScriptsCorner = Instance.new("UICorner")
ScriptsCorner.CornerRadius = UDim.new(0, 8)
ScriptsCorner.Parent = ScriptsPanel

local ScriptsHeader = Instance.new("Frame")
ScriptsHeader.Name = "ScriptsHeader"
ScriptsHeader.Size = UDim2.new(1, 0, 0, 40)
ScriptsHeader.BackgroundColor3 = Color3.fromRGB(35, 35, 40)
ScriptsHeader.BorderSizePixel = 0

local ScriptsTitle = Instance.new("TextLabel")
ScriptsTitle.Name = "ScriptsTitle"
ScriptsTitle.Size = UDim2.new(0, 200, 1, 0)
ScriptsTitle.Position = UDim2.new(0, 15, 0, 0)
ScriptsTitle.BackgroundTransparency = 1
ScriptsTitle.Text = "📁 PSF SCRIPTS"
ScriptsTitle.TextColor3 = Color3.fromRGB(220, 220, 255)
ScriptsTitle.TextSize = 14
ScriptsTitle.Font = Enum.Font.GothamBold
ScriptsTitle.TextXAlignment = Enum.TextXAlignment.Left

-- Поиск
local SearchBox = Instance.new("TextBox")
SearchBox.Name = "SearchBox"
SearchBox.Size = UDim2.new(1, -20, 0, 35)
SearchBox.Position = UDim2.new(0, 10, 0, 50)
SearchBox.BackgroundColor3 = Color3.fromRGB(35, 35, 45)
SearchBox.PlaceholderText = "🔍 Search PSF scripts..."
SearchBox.Text = ""
SearchBox.TextColor3 = Color3.fromRGB(220, 220, 220)
SearchBox.TextSize = 13
SearchBox.Font = Enum.Font.Gotham

local SearchCorner = Instance.new("UICorner")
SearchCorner.CornerRadius = UDim.new(0, 6)
SearchCorner.Parent = SearchBox

-- Список скриптов
local ScriptsList = Instance.new("ScrollingFrame")
ScriptsList.Name = "ScriptsList"
ScriptsList.Size = UDim2.new(1, -20, 1, -150)
ScriptsList.Position = UDim2.new(0, 10, 0, 95)
ScriptsList.BackgroundTransparency = 1
ScriptsList.ScrollBarThickness = 6
ScriptsList.CanvasSize = UDim2.new(0, 0, 0, 800)

local ListLayout = Instance.new("UIListLayout")
ListLayout.Padding = UDim.new(0, 8)
ListLayout.SortOrder = Enum.SortOrder.LayoutOrder

-- Кнопка сворачивания в левом нижнем углу
local ToggleButton = Instance.new("ImageButton")
ToggleButton.Name = "ToggleButton"
ToggleButton.Size = UDim2.new(0, 40, 0, 40)
ToggleButton.Position = UDim2.new(0, 10, 1, -50)
ToggleButton.BackgroundColor3 = Color3.fromRGB(60, 60, 80)
ToggleButton.Image = "rbxassetid://137986135220752"
ToggleButton.ScaleType = Enum.ScaleType.Fit

local ToggleCorner = Instance.new("UICorner")
ToggleCorner.CornerRadius = UDim.new(0, 8)
ToggleCorner.Parent = ToggleButton

-- Сборка интерфейса
ContainerShadow.Parent = MainContainer
ScreenGui.Parent = playerGui
MainContainer.Parent = ScreenGui
Header.Parent = MainContainer
Title.Parent = Header
SubTitle.Parent = Header
Content.Parent = MainContainer
EditorPanel.Parent = Content
EditorHeader.Parent = EditorPanel
EditorTitle.Parent = EditorHeader
EditorContainer.Parent = EditorPanel
EditorContainerCorner.Parent = EditorContainer
EditorScroll.Parent = EditorContainer
EditorBox.Parent = EditorScroll
EditorTools.Parent = EditorPanel
ExecuteBtn.Parent = EditorTools
ClearBtn.Parent = EditorTools
ScriptsPanel.Parent = Content
ScriptsHeader.Parent = ScriptsPanel
ScriptsTitle.Parent = ScriptsHeader
SearchBox.Parent = ScriptsPanel
ScriptsList.Parent = ScriptsPanel
ListLayout.Parent = ScriptsList
ToggleButton.Parent = MainContainer

-- ДАННЫЕ СКРИПТОВ PSF REMAKE 3.0
local scriptsData = {
    {
        name = "🌟 PSF Remake 3.0", 
        content = [[loadstring(game:HttpGet("https://raw.githubusercontent.com/Dipperlop/PSF-Remake-2.0/main/PSF%20Remake.lua"))()]],
        color = Color3.fromRGB(0, 100, 255),
        icon = "🌟"
    },
    {
        name = "🔨 PSF Ban System", 
        content = [[-- PSF Ban System v3.0
local PSF = {Version = "3.0", Premium = true}
function PSF:BanPlayer(target)
    print("🔨 PSF Banning: " .. target)
    return "Banned successfully"
end
print("PSF Ban System loaded!")]],
        color = Color3.fromRGB(255, 80, 80),
        icon = "🔨"
    },
    {
        name = "⚡ PSF Auto Farm", 
        content = [[-- PSF Auto Farm v3.0
local PSF = {Version = "3.0", Farming = false}
function PSF:StartFarm()
    self.Farming = true
    print("⚡ PSF Auto Farm activated!")
end
PSF:StartFarm()]],
        color = Color3.fromRGB(80, 180, 255),
        icon = "⚡"
    },
    {
        name = "🛡️ PSF Anti-Cheat", 
        content = [[-- PSF Anti-Cheat v3.0
local PSF = {Version = "3.0", Protection = true}
function PSF:Scan()
    print("🛡️ PSF Anti-Cheat scanning...")
    return "System secure"
end
PSF:Scan()]],
        color = Color3.fromRGB(100, 255, 100),
        icon = "🛡️"
    },
    {
        name = "🎯 PSF Aim Assist", 
        content = [[-- PSF Aim Assist v3.0
local PSF = {Version = "3.0", AimAssist = true}
function PSF:ToggleAim()
    self.AimAssist = not self.AimAssist
    print("🎯 PSF Aim Assist: " .. tostring(self.AimAssist))
end
PSF:ToggleAim()]],
        color = Color3.fromRGB(255, 180, 0),
        icon = "🎯"
    },
    {
        name = "R6 Script", 
        content = [[loadstring(game:HttpGet("https://raw.githubusercontent.com/sunday102711-bit/E00KIDD-r6-by-me/refs/heads/main/R6%20Real"))()]],
        color = Color3.fromRGB(120, 80, 200),
        icon = "👤"
    },
    {
        name = "Re Script", 
        content = [[if game.Players.LocalPlayer.Character then
    game.Players.LocalPlayer.Character.Humanoid.Health = 0
end]],
        color = Color3.fromRGB(200, 60, 60),
        icon = "💀"
    },
    {
        name = "Primadon", 
        content = [[loadstring(game:HttpGet("https://pastebin.com/raw/37iQzQp3"))()]],
        color = Color3.fromRGB(60, 180, 120),
        icon = "👑"
    },
    {
        name = "Grandosla", 
        content = [[loadstring(game:HttpGet("https://pastefy.app/Kql9VClT/raw"))()]],
        color = Color3.fromRGB(180, 120, 60),
        icon = "🐉"
    },
    {
        name = "Allah ARABIC", 
        content = [[loadstring(game:HttpGet("https://pastebin.com/raw/NNp1NJKT"))()]],
        color = Color3.fromRGB(40, 140, 200),
        icon = "☪️"
    },
    {
        name = "ExSeR", 
        content = [[loadstring(game:HttpGet("https://pastefy.app/7sO8ckTL/raw",true))()]],
        color = Color3.fromRGB(200, 100, 40),
        icon = "⚔️"
    },
    {
        name = "Topk3k", 
        content = [[loadstring(game:HttpGet('https://raw.githubusercontent.com/radiuscool/topkek/refs/heads/main/adsdsaadsdassad'))()]],
        color = Color3.fromRGB(160, 80, 180),
        icon = "🔝"
    },
    {
        name = "Patrilck Duck", 
        content = [[loadstring(game:HttpGet("https://pastefy.app/faAQ2rsV/raw"))()]],
        color = Color3.fromRGB(80, 160, 200),
        icon = "🦆"
    },
    {
        name = "Disco Lights", 
        content = [[while true do
    game.Lighting.Ambient = Color3.new(math.random(), math.random(), math.random())
    wait(0.2)
    game.Lighting.ShadowColor = Color3.new(math.random(), math.random(), math.random())
    wait(0.2) 
end]],
        color = Color3.fromRGB(200, 60, 160),
        icon = "🌈"
    },
    {
        name = "Polaria", 
        content = [[loadstring(game:HttpGet("https://pastefy.app/agqEB2qs/raw",true))()]],
        color = Color3.fromRGB(60, 200, 160),
        icon = "❄️"
    },
    {
        name = "Bipolaria", 
        content = [[loadstring(game:HttpGet("https://pastefy.app/agqEB2qs/raw",true))()]],
        color = Color3.fromRGB(160, 60, 200),
        icon = "🌓"
    },
    {
        name = "Scythe", 
        content = [[loadstring(game:HttpGet("https://raw.githubusercontent.com/gitezgitgit/rare-scripts/refs/heads/main/Scythe.txt"))()]],
        color = Color3.fromRGB(40, 40, 40),
        icon = "⚰️"
    },
    {
        name = "Freddy", 
        content = [[loadstring(game:HttpGet("https://raw.githubusercontent.com/gitezgitgit/rare-scripts/refs/heads/main/FreddyFazBear.txt"))()]],
        color = Color3.fromRGB(180, 60, 40),
        icon = "🐻"
    },
    {
        name = "Soul Reaper", 
        content = [[loadstring(game:HttpGet("https://raw.githubusercontent.com/gObl00x/My-Converts/refs/heads/main/Soul%20Reaper.lua"))()]],
        color = Color3.fromRGB(80, 20, 120),
        icon = "💀"
    },
    {
        name = "Goner", 
        content = [[loadstring(game:HttpGet("https://raw.githubusercontent.com/gObl00x/My-Converts/refs/heads/main/Goner.lua"))()]],
        color = Color3.fromRGB(120, 40, 160),
        icon = "👻"
    },
    {
        name = "MrByeBye", 
        content = [[loadstring(game:HttpGet("https://raw.githubusercontent.com/gObl00x/My-Converts/refs/heads/main/Mr.Bye%20Bye.lua"))()]],
        color = Color3.fromRGB(200, 40, 40),
        icon = "👋"
    },
    {
        name = "Mr.Pixels", 
        content = [[loadstring(game:HttpGet("https://raw.githubusercontent.com/gObl00x/My-Converts/refs/heads/main/Mr.Pixels.lua"))()]],
        color = Color3.fromRGB(60, 120, 200),
        icon = "👨‍💻"
    },
    {
        name = "Logo", 
        content = [[loadstring(game:HttpGet("https://pastefy.app/ag3eykh3/raw"))()]],
        color = Color3.fromRGB(200, 160, 40),
        icon = "🔰"
    },
    {
        name = "HeadName", 
        content = [[loadstring(game:HttpGet("https://pastefy.app/KvEz3BLH/raw"))()]],
        color = Color3.fromRGB(100, 200, 100),
        icon = "👤"
    },
    {
        name = "Decal", 
        content = [[loadstring(game:HttpGet("https://pastefy.app/BarK7bYH/raw"))()]],
        color = Color3.fromRGB(180, 100, 200),
        icon = "🖼️"
    },
    {
        name = "Kick", 
        content = [[loadstring(game:HttpGet("https://pastefy.app/ZEfG2ZI6/raw"))()]],
        color = Color3.fromRGB(255, 80, 80),
        icon = "👢"
    }
}

-- Функция создания элемента скрипта
local function createScriptEntry(scriptData, index)
    local entry = Instance.new("Frame")
    entry.Name = "ScriptEntry"
    entry.Size = UDim2.new(1, 0, 0, 70)
    entry.BackgroundColor3 = Color3.fromRGB(35, 35, 45)
    entry.LayoutOrder = index
    
    local entryCorner = Instance.new("UICorner")
    entryCorner.CornerRadius = UDim.new(0, 8)
    entryCorner.Parent = entry
    
    local iconLabel = Instance.new("TextLabel")
    iconLabel.Name = "Icon"
    iconLabel.Size = UDim2.new(0, 40, 0, 40)
    iconLabel.Position = UDim2.new(0, 10, 0.5, -20)
    iconLabel.BackgroundTransparency = 1
    iconLabel.Text = scriptData.icon
    iconLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
    iconLabel.TextSize = 20
    iconLabel.Font = Enum.Font.GothamBold
    
    local nameLabel = Instance.new("TextLabel")
    nameLabel.Name = "Name"
    nameLabel.Size = UDim2.new(1, -60, 0, 20)
    nameLabel.Position = UDim2.new(0, 55, 0, 10)
    nameLabel.BackgroundTransparency = 1
    nameLabel.Text = scriptData.name
    nameLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
    nameLabel.TextSize = 13
    nameLabel.Font = Enum.Font.GothamBold
    nameLabel.TextXAlignment = Enum.TextXAlignment.Left
    
    local loadBtn = Instance.new("TextButton")
    loadBtn.Name = "LoadBtn"
    loadBtn.Size = UDim2.new(0, 60, 0, 25)
    loadBtn.Position = UDim2.new(0, 55, 1, -30)
    loadBtn.BackgroundColor3 = scriptData.color
    loadBtn.Text = "LOAD"
    loadBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
    loadBtn.TextSize = 11
    loadBtn.Font = Enum.Font.GothamBold
    
    local loadCorner = Instance.new("UICorner")
    loadCorner.CornerRadius = UDim.new(0, 4)
    loadCorner.Parent = loadBtn
    
    -- Добавляем элементы
    iconLabel.Parent = entry
    nameLabel.Parent = entry
    loadBtn.Parent = entry
    
    -- Функционал кнопки
    loadBtn.MouseButton1Click:Connect(function()
        EditorBox.Text = scriptData.content
        showNotification("📥 Loaded: " .. scriptData.name, scriptData.color)
    end)
    
    return entry
end

-- Функция показа уведомлений
local function showNotification(message, color)
    local notification = Instance.new("Frame")
    notification.Name = "Notification"
    notification.Size = UDim2.new(0, 300, 0, 40)
    notification.Position = UDim2.new(0.5, -150, 0, 10)
    notification.AnchorPoint = Vector2.new(0.5, 0)
    notification.BackgroundColor3 = color
    notification.BorderSizePixel = 0
    
    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, 8)
    corner.Parent = notification
    
    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(1, 0, 1, 0)
    label.BackgroundTransparency = 1
    label.Text = message
    label.TextColor3 = Color3.fromRGB(255, 255, 255)
    label.TextSize = 13
    label.Font = Enum.Font.GothamBold
    label.Parent = notification
    
    notification.Parent = MainContainer
    
    -- Анимация
    notification.BackgroundTransparency = 1
    label.TextTransparency = 1
    
    TweenService:Create(notification, TweenInfo.new(0.3), {BackgroundTransparency = 0}):Play()
    TweenService:Create(label, TweenInfo.new(0.3), {TextTransparency = 0}):Play()
    
    wait(2)
    
    TweenService:Create(notification, TweenInfo.new(0.3), {BackgroundTransparency = 1}):Play()
    TweenService:Create(label, TweenInfo.new(0.3), {TextTransparency = 1}):Play()
    
    wait(0.3)
    notification:Destroy()
end

-- Заполнение списка скриптов
local function populateScriptsList()
    for _, child in ipairs(ScriptsList:GetChildren()) do
        if child:IsA("Frame") then
            child:Destroy()
        end
    end
    
    for i, scriptData in ipairs(scriptsData) do
        local entry = createScriptEntry(scriptData, i)
        entry.Parent = ScriptsList
    end
    
    -- Обновление размера канваса
    task.wait()
    local totalHeight = 0
    for _, child in ipairs(ScriptsList:GetChildren()) do
        if child:IsA("Frame") then
            totalHeight += child.AbsoluteSize.Y + 8
        end
    end
    ScriptsList.CanvasSize = UDim2.new(0, 0, 0, totalHeight)
end

-- Поиск скриптов
SearchBox:GetPropertyChangedSignal("Text"):Connect(function()
    local searchText = string.lower(SearchBox.Text)
    
    for _, entry in ipairs(ScriptsList:GetChildren()) do
        if entry:IsA("Frame") then
            local nameLabel = entry:FindFirstChild("Name")
            if nameLabel then
                if string.lower(nameLabel.Text):find(searchText) or searchText == "" then
                    entry.Visible = true
                else
                    entry.Visible = false
                end
            end
        end
    end
end)

-- Функционал кнопок
ExecuteBtn.MouseButton1Click:Connect(function()
    local content = EditorBox.Text
    local success, err = pcall(function()
        loadstring(content)()
    end)
    if not success then
        showNotification("❌ Error: " .. err, Color3.fromRGB(255, 80, 80))
    else
        showNotification("🚀 Script Executed - Check Output (F9)", Color3.fromRGB(0, 200, 100))
    end
end)

ClearBtn.MouseButton1Click:Connect(function()
    EditorBox.Text = ""
    showNotification("🗑️ Editor Cleared", Color3.fromRGB(255, 150, 0))
end)

-- ПЕРЕМЕЩЕНИЕ ОКНА
local dragging = false
local dragInput
local dragStart
local startPos

local function update(input)
    local delta = input.Position - dragStart
    MainContainer.Position = UDim2.new(
        startPos.X.Scale, 
        startPos.X.Offset + delta.X,
        startPos.Y.Scale, 
        startPos.Y.Offset + delta.Y
    )
end

Header.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        dragging = true
        dragStart = input.Position
        startPos = MainContainer.Position
        
        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then
                dragging = false
            end
        end)
    end
end)

Header.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement then
        dragInput = input
    end
end)

UserInputService.InputChanged:Connect(function(input)
    if input == dragInput and dragging then
        update(input)
    end
end)

-- ПЛАВНОЕ СВОРАЧИВАНИЕ/РАЗВОРАЧИВАНИЕ
local isMinimized = false
local originalSize = MainContainer.Size
local originalPosition = MainContainer.Position

ToggleButton.MouseButton1Click:Connect(function()
    if isMinimized then
        -- РАЗВОРАЧИВАНИЕ
        showNotification("📤 Restoring PSF Window...", Color3.fromRGB(0, 200, 100))
        
        MainContainer.Size = UDim2.new(0, 50, 0, 50)
        MainContainer.Visible = true
        
        TweenService:Create(MainContainer, TweenInfo.new(0.5, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
            Size = originalSize
        }):Play()
        
        TweenService:Create(MainContainer, TweenInfo.new(0.5, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
            Position = originalPosition
        }):Play()
        
        TweenService:Create(Content, TweenInfo.new(0.3), {
            BackgroundTransparency = 1
        }):Play()
        
        isMinimized = false
    else
        -- СВОРАЧИВАНИЕ
        showNotification("📥 Minimizing PSF Window...", Color3.fromRGB(255, 150, 0))
        
        originalSize = MainContainer.Size
        originalPosition = MainContainer.Position
        
        TweenService:Create(MainContainer, TweenInfo.new(0.6, Enum.EasingStyle.Quad, Enum.EasingDirection.In), {
            Size = UDim2.new(0, 50, 0, 50),
            Position = UDim2.new(0, 10, 1, -60)
        }):Play()
        
        TweenService:Create(Content, TweenInfo.new(0.4), {
            BackgroundTransparency = 0.8
        }):Play()
        
        isMinimized = true
    end
end)

-- Инициализация
populateScriptsList()

-- Анимация появления при загрузке
MainContainer.Position = UDim2.new(0.5, -400, 0.3, -300)
TweenService:Create(MainContainer, TweenInfo.new(0.7, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
    Position = originalPosition
}):Play()

showNotification("🚀 PSF Remake 3.0 Loaded!", Color3.fromRGB(0, 150, 255))

print("🚀 PSF REMAKE 3.0")
print("📁 Ultimate Script Hub")
print("🎯 Premium Design + Smooth Animations")
print("🔧 Toggle Button: Bottom Left Corner")
print("📍 All scripts integrated perfectly")

return ScreenGui
